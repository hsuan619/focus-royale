[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["cd server && npm ci"]

[phases.build]
cmds = ["cd server && npx prisma generate && npx prisma migrate deploy"]

[start]
cmd = "cd server && npm start"
